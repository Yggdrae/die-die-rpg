/**
 * Test and development doubles.
 *
 * Never imported by production code paths. Feature 03 replaces the repository double with
 * the real local-first implementation.
 */
export * from './in-memory-repository.ts';
