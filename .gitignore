node_modules/
dist/
build/
*.tsbuildinfo

.env
.env.local
.env.*.local

.DS_Store
Thumbs.db

coverage/
test-results/
playwright-report/
apps/web/public/sync-assets/
.vite/
dev-dist/

*.log

# Spike only — vendored sqlite-wasm distribution, regenerable (see spike/README.md).
# The spike itself is committed so its findings are auditable, and deleted at the freeze tag.
spike/public/sqlite/
spike/dist/
